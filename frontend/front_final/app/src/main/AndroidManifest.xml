<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ro_fa.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.CrearUsuarioActivity" />
        <activity
            android:name=".activities.NavActivity"
            android:label="@string/title_activity_nav"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.ListarUsuariosActivity" />
        <activity android:name=".activities.VerProyectosActivity" />
        <activity android:name=".activities.DatosProyectoActivity" />
        <activity android:name=".activities.CrearProyectoActivity" />
        <activity android:name=".activities.EditarProyectoActivity" />
        <activity android:name=".activities.CrearUSActivity" />
        <activity android:name=".activities.AdministrarProyectosActivity" />
        <activity android:name=".activities.AccionesProyectoActivity" />
        <activity android:name=".activities.AsignarUsuarioActivity" />
        <activity android:name=".activities.CrearSprintActivity" />
        <activity android:name=".activities.VerSprintsActivity" />
        <activity android:name=".activities.VerUSActivity" />
        <activity android:name=".activities.VerUsuarioActivity"></activity>
    </application>

</manifest>